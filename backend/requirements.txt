# --- Web framework ---
fastapi==0.115.6
uvicorn[standard]==0.34.0

# --- Settings & validation (Pydantic v2) ---
pydantic==2.10.4
pydantic-settings==2.7.1

# --- Database (SQLAlchemy 2.0 + async/sync Postgres drivers) ---
sqlalchemy==2.0.36
alembic==1.14.0
asyncpg==0.30.0      # async driver used by the running application
psycopg[binary]==3.2.3  # sync driver used by Alembic migrations

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.2.1
python-multipart==0.0.20   # required by FastAPI for OAuth2PasswordRequestForm (Auth module)

# --- External integrations (AI provider, Google Calendar, Salesforce/HubSpot CRM) ---
httpx==0.28.1
aiosmtplib==3.0.2   # async SMTP client for the Email integration layer

# --- Misc ---
email-validator==2.2.0
